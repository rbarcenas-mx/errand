---
description: Validate (lint + build + tests), optionally review code with pr-review-expert, push commits to GitHub, and monitor CI status.
---

## User Input

```text
$ARGUMENTS
```

## Goal

Load the skill `ci.ship` using the `skill` tool and execute the steps described in SKILL.md.
